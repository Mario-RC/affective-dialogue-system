# Export these variables in your shell, or load them in your application.
# The CLI does not automatically read .env files.
ADS_DEVICE=cuda:0
ADS_CACHE_DIR=/path/to/huggingface/cache
ADS_SPEAKER_WAV=/path/to/reference_speaker.wav

